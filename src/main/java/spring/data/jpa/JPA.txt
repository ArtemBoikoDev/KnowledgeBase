JOIN vs JOIN FETCH
оба работают одинаково, если связь не является коллекцией, а единичная,
оба варианта сводят количество запросов к одному,
но если связь коллекция, то она хибером смаплена не будет,
причём SQL запрос будет такой же, и база вернёт всё то же самое,
но уже хибер НЕ БДУЕТ мапить эту коллекцию в поле сущности


@OneToOne
делать через
https://www.baeldung.com/jpa-one-to-one
4. Using a Shared Primary Key


org.modelmapper
очень медленный, в разы быстрее делать через ObjectMapper из Jackson